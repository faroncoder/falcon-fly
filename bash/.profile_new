# /home/faron/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if /home/faron/.bash_profile or /home/faron/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022
MOI=`whoami`
if [ "$MOI" = "root" ];
	then
	#let check to be sure
	if [ "`echo $EUID`" = 0 ];
		then
			MYHOME=/$MOI
	fi
else
	if [ "`echo $EUID`" < 1000 ];
		then
			MYHOME=/home/$MOI
	fi
fi
# if running bash
if [ -n "$BASH_VERSION" ];
	then
    	# include .bashrc if it exists
    	if [ -f "$MYHOME/.bashrc" ];
    		then
				. $MYHOME/.bashrc
    	fi
fi
# set PATH so it includes user's private bin if it exists
if [ -d "$MYHOME/bin" ];
	then
		export PATH=$PATH:$MYHOME/bin
fi

if [ -d "$MYHOME/.rvm/bin" ];
	then
		export PATH=$PATH:$MYHOME/.rvm/bin
		##	export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting
		[[ -s "$MYHOME/.rvm/scripts/rvm" ]] && source "$MYHOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
fi
