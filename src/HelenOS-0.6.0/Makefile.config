#########################################
## AUTO-GENERATED FILE, DO NOT EDIT!!! ##
## Generated by: tools/config.py       ##
#########################################

# Platform
PLATFORM = amd64

# CPU type
PROCESSOR = opteron

# RAM disk format
RDFMT = ext4fs

# Kernel architecture
KARCH = amd64

# User space architecture
UARCH = amd64

# Boot architecture
BARCH = amd64

# Compiler
COMPILER = clang

# Fences
CONFIG_FENCES_P4 = y

# IOMAP bitmap support
CONFIG_IOMAP_BITMAP = y

# ACPI support
CONFIG_ACPI = y

# Hierarchical page tables support
CONFIG_PAGE_PT = y

# Multiboot standard support
CONFIG_MULTIBOOT = y

# FPU support
CONFIG_FPU = y

# Support for SMP
CONFIG_SMP = y

# Debug build
CONFIG_DEBUG = y

# Deadlock detection support for spinlocks
CONFIG_DEBUG_SPINLOCK = y

# Lazy FPU context switching
CONFIG_FPU_LAZY = y

# Support for userspace debuggers
CONFIG_UDEBUG = y

# Kernel console support
CONFIG_KCONSOLE = y

# Kernel symbol information
CONFIG_SYMTAB = y

# Detailed kernel logging
CONFIG_LOG = y

# Kernel function tracing
CONFIG_TRACE = y

# Compile kernel tests
CONFIG_TEST = y

# Input device class
CONFIG_HID_IN = generic

# Output device class
CONFIG_HID_OUT = generic

# PC keyboard support
CONFIG_PC_KBD = y

# Support for NS16550 controller (kernel console)
CONFIG_NS16550_KCON = n

# Support for i8042 controller
CONFIG_I8042 = y

# EGA support
CONFIG_EGA = y

# Framebuffer support
CONFIG_FB = y

# Default framebuffer mode
CONFIG_BFB_MODE = 1280x800

# Default framebuffer depth
CONFIG_BFB_BPP = 24

# 
CONFIG_RTLD = n

# 
CONFIG_BUILD_SHARED_LIBS = n

# 
CONFIG_USE_SHARED_LIBS = n

# Launch (devman) test drivers
CONFIG_TEST_DRIVERS = n

# Write core files
CONFIG_WRITE_CORE_FILES = y

# Include userspace unit tests (PCUT)
CONFIG_PCUT_TESTS = n

# Include PCUT self-tests
CONFIG_PCUT_SELF_TESTS = n

# Include development files (headers, libraries)
CONFIG_DEVEL_FILES = y

# Strip binaries
CONFIG_STRIP_BINARIES = y

# Optimize for size
CONFIG_OPTIMIZE_FOR_SIZE = y

# Barebone build with essential binaries only
CONFIG_BAREBONE = y

# Start virtual USB host controller
CONFIG_RUN_VIRTUAL_USB_HC = y

# OHCI root hub port power switching
OHCI_POWER_SWITCH = no

# GRUB boot loader architecture
GRUB_ARCH = pc

# Default framebuffer width
CONFIG_BFB_WIDTH = 1280

# Default framebuffer height
CONFIG_BFB_HEIGHT = 800

REVISION = unknown (unknown)
TIMESTAMP_UNIX = 1434491812
TIMESTAMP = 2015-06-16 17:56:52
CONFIG_DEFS = -DPLATFORM=amd64 -DPLATFORM_amd64 -DPROCESSOR=opteron -DPROCESSOR_opteron -DRDFMT=ext4fs -DRDFMT_ext4fs -DKARCH=amd64 -DKARCH_amd64 -DUARCH=amd64 -DUARCH_amd64 -DBARCH=amd64 -DBARCH_amd64 -DCOMPILER=clang -DCOMPILER_clang -DCONFIG_FENCES_P4 -DCONFIG_IOMAP_BITMAP -DCONFIG_ACPI -DCONFIG_PAGE_PT -DCONFIG_MULTIBOOT -DCONFIG_FPU -DCONFIG_SMP -DCONFIG_DEBUG -DCONFIG_DEBUG_SPINLOCK -DCONFIG_FPU_LAZY -DCONFIG_UDEBUG -DCONFIG_KCONSOLE -DCONFIG_SYMTAB -DCONFIG_LOG -DCONFIG_TRACE -DCONFIG_TEST -DCONFIG_HID_IN=generic -DCONFIG_HID_IN_generic -DCONFIG_HID_OUT=generic -DCONFIG_HID_OUT_generic -DCONFIG_PC_KBD -DCONFIG_I8042 -DCONFIG_EGA -DCONFIG_FB -DCONFIG_BFB_MODE=1280x800 -DCONFIG_BFB_MODE_1280x800 -DCONFIG_BFB_BPP=24 -DCONFIG_BFB_BPP_24 -DCONFIG_WRITE_CORE_FILES -DCONFIG_DEVEL_FILES -DCONFIG_STRIP_BINARIES -DCONFIG_OPTIMIZE_FOR_SIZE -DCONFIG_BAREBONE -DCONFIG_RUN_VIRTUAL_USB_HC -DOHCI_POWER_SWITCH=no -DOHCI_POWER_SWITCH_no -DGRUB_ARCH=pc -DGRUB_ARCH_pc -DCONFIG_BFB_WIDTH=1280 -DCONFIG_BFB_WIDTH_1280 -DCONFIG_BFB_HEIGHT=800 -DCONFIG_BFB_HEIGHT_800 "-DREVISION=unknown (unknown)" "-DTIMESTAMP_UNIX=1434491812"
 "-DTIMESTAMP=2015-06-16 17:56:52"
